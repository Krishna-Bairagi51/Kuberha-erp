"use client"

export const COLLECTION_NAME_REGEX = /^[A-Za-z0-9\s]+$/
export const COLLECTION_NAME_ERROR = 'Collection name cannot contain special characters'
export const CATEGORY_NAME_REGEX = /^[A-Za-z0-9\s]+$/
export const CATEGORY_NAME_ERROR = 'Category name cannot contain special characters'

// Explicit export to ensure module detection
export {}

